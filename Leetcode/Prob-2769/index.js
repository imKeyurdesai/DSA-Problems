/**
 * @param {number} num
 * @param {number} t
 * @return {number}
 */
var theMaximumAchievableX = function(num, t) {
    return num + t*2
};

const num = 4
const t = 1
console.log(theMaximumAchievableX(num,t))